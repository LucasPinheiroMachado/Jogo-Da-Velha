# Jogo da Velha

Jogo feito com HTML, CSS e TypeScript para ser jogado entre dois jogadores localmente!

# Como rodar? 

Você precisa ter o **"node"** instalado e dar os comandos **"npm install"** e em seguida **"npx tsc"**.
